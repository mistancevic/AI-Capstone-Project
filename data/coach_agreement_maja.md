# Coaching Agreement — Maja × Coach Dana (synthetic)

Package: Light (digest opt-out variant — exercises the flag-scope setting).

```
channel: in-app coach inbox
response_window_hours: 48
quiet_hours: 22:00-06:30
flag_scope: urgent_only
hard_stop_override: none
```

Plain-language summary: only urgent safety flags reach the coach; digest
entries are kept in the app but not delivered (the package the client
chose). Quiet hours delay notification, never protection — the client-side
stop message and safe default are always immediate.
